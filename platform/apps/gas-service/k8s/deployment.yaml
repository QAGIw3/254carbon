apiVersion: apps/v1
kind: Deployment
metadata:
  name: gas-service
  namespace: market-intelligence
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gas-service
  template:
    metadata:
      labels:
        app: gas-service
    spec:
      containers:
      - name: gas-service
        image: 254carbon/gas-service:latest
        ports:
        - containerPort: 8011
        env:
        - name: CLICKHOUSE_HOST
          value: "clickhouse"
        - name: CLICKHOUSE_PORT
          value: "9000"
        - name: POSTGRES_HOST
          value: "postgresql"
        - name: POSTGRES_PORT
          value: "5432"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8011
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8011
          initialDelaySeconds: 60
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: gas-service
  namespace: market-intelligence
spec:
  selector:
    app: gas-service
  ports:
  - port: 8011
    targetPort: 8011
  type: ClusterIP
