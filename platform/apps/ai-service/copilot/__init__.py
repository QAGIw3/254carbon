"""
Copilot Engine package

Provides the core conversational engine used by the `copilot` router.
Implements multi-model selection, simple entity extraction, retrieval-
augmented generation (RAG) placeholders, and message construction for LLMs.

Replace in-memory state with persistent storage (e.g., Redis) for production.
"""
